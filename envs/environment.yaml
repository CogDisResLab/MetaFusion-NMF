name: snf_nmf_env
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - python=3.9
  - pandas>=1.5.0
  - numpy>=1.23.0
  - scikit-learn>=1.2.0
  - snakemake>=7.0.0
  - matplotlib  # Added for the visualization step we discussed
  - seaborn     # Added for high-quality cluster heatmaps
  - pip
  - mygene
  - pip:
    - snfpy==0.2.2
